package com.example.demo.appuser;

/**
 * Represents the roles available for application users.
 * Users can have either the USER role or the ADMIN role.
 */
public enum AppUserRole {
    USER, ADMIN;
}
